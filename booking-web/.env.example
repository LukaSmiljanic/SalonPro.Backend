# Pun URL do SalonPro API (isti kao admin app). Za dev sa Vite proxy može ostati prazno i koristiti relativne /api pozive.
# Lokalno: SalonPro.API često na http://localhost:5001 (vidi launchSettings.json)
VITE_API_BASE_URL=http://localhost:5001
